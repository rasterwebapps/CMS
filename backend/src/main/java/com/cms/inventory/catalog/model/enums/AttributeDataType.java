package com.cms.inventory.catalog.model.enums;

/** The kind of value a {@code CategoryAttribute} accepts on a {@code Product}. */
public enum AttributeDataType {
    TEXT,
    NUMBER,
    DATE,
    BOOLEAN,
    ENUM
}

package com.cms.inventory.approval.model.enums;

/** Lifecycle of one {@code ApprovalAction} — a single step's sign-off within one instance. */
public enum ApprovalActionStatus {
    PENDING,
    APPROVED,
    REJECTED
}

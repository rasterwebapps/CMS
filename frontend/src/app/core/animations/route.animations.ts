/**
 * Route transitions are configured through `provideRouter(routes, withViewTransitions())`
 * in `app.config.ts`.
 *
 * This module intentionally avoids legacy Angular animation metadata because that API is
 * deprecated in Angular 21 and scheduled for removal in Angular 23.
 */
export {};

package com.cms.inventory.indent.model.enums;

/** Header lifecycle — same shape as {@code PurchaseRequisitionStatus} (the closest in-repo precedent). */
public enum StockIndentStatus {
    DRAFT,
    SUBMITTED,
    COMPLETED,
    CANCELLED
}

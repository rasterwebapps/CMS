package com.cms.model.enums;

public enum ElectiveSelectionMode {
    /** Each student individually picks their offering — assigned one at a time via Elective Assignment. */
    STUDENT_CHOICE,
    /** The institution centrally decides one offering for the whole group — bulk-applied to every eligible student. */
    INSTITUTION_DECIDED
}

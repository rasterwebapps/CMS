package com.cms.inventory.approval.model.enums;

/** Lifecycle of one {@code ApprovalInstance} (a document actually routed through a workflow). */
public enum ApprovalInstanceStatus {
    IN_PROGRESS,
    APPROVED,
    REJECTED
}
